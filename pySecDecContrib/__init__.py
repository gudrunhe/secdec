# This dummy is here so that we could import the contrib module,
# and find out the full path to this directory, wherever it was
# installed to.

import os.path

dirname = os.path.abspath(os.path.dirname(__file__))

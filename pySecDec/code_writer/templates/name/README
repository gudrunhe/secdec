This code has been generated by `pySecDec-%(pySecDec_version)s`
(commit id: %(pySecDec_git_id)s)
on "%(date_time)s".

If you have problems building the library, make sure that you
compile and link against the dependencies shipped with the
`pySecDec-%(pySecDec_version)s` tarball.
